# Runtime dependencies for the Vercel serverless function (backend.api.main:app).
# Keep this list lean: Vercel bundles every installed package with no
# tree-shaking and enforces a 500 MB function limit. Pipeline-only packages and
# dev tools live in requirements-dev.txt and never ship to production.
fastapi
uvicorn
pydantic
pydantic-settings
httpx
psycopg[binary]
scipy
google-genai>=1.0
