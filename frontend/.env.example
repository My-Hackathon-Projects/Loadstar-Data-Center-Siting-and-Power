# Optional. Leave empty for same-origin (Vite proxy in dev, FastAPI static serve in prod).
# Set to `http://127.0.0.1:8000` (or another origin) when developing the SPA against a remote API.
VITE_API_BASE_URL=
