# Generated by `make layer-assets`. Tracked locally for the dev server but kept
# out of git so the static path always reflects the latest pipeline output.
*.json
# Exception: the PyPSA-Eur HV grid overlay has no source on Vercel (it is built
# from local-only raw CSVs), so the derived artifact is committed to ship it.
!transmission_grid.json
